name: shared_models
description: Shared data models and types for PhoneCam ecosystem.
version: 1.0.0
homepage: https://github.com/phonecam/phonecam

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
